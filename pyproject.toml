[project]
name = "omni"
version = "0.7.0"
description = "Autonomous Minecraft agent with a local LLM and deterministic tool layer"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ollama",
    "javascript",
    "pymorphy3",
    "pymorphy3-dicts-ru",
]
